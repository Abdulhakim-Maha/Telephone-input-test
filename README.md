# Telephon number input

This repository is craeted to solved Telephone number input by using React + Vite

To run this project:

```bash
docker-compose up
```

or

```bash
npm install
npm run dev
```

Website is running on `localhost:3000`
